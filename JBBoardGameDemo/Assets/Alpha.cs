using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Alpha : MonoBehaviour
{
    // Start is called before the first frame update
    public bool ChallengeCall()
    {
        return true;
    }

    public bool EscapeCall()
    {
        return true;
    }
}
